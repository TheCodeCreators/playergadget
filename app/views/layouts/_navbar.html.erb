<nav class="navbar navbar-default transparent navbar-fixed-top my-navbar">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="brand my-brand">
        <a class="navbar-brand" href="/">
           <img src="/images/header_logo.png">
        </a>
      </div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to "BLOG", articles_path %>
        </li>
        <li>
          <%= link_to 'WHO WE ARE', root_path(anchor: "who-we-are") %>
        </li>
        <li class="">
          <%= link_to 'WHAT WE DO', root_path(anchor: "what-we-do") %>
        </li>
        <li>
          <%= link_to 'CONTACT US', root_path(anchor: "contact-us") %>
        </li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
              <div class="user-avatar-navbar">
                <span class="user-name-navbar blue-link">
                  Welcome, <%= current_user.name %>
                </span>
                <span class="caret"></span>
              </div>
            </a>
            <ul class="dropdown-menu inverse-dropdown" role="menu">
              <% if current_user.admin? %>
                <li>
                  <a href="/admin">Admin Area</a>
                </li>
                <li class="divider">
                </li>
              <% end %>
              <li>
                <%= link_to "Logout", main_app.destroy_user_session_path,
                 method: :delete, :class => 'navbar-link'  %>
              </li>
            </ul>
        <% else %>
          <li>
            <span class="navbar-text blue-link" data-toggle="modal" data-target="#loginModal">
              SIGN IN
            </span>
          </li>
          <li>
            <span class="navbar-text blue-link" data-toggle="modal" data-target="#signUpModal">
              SIGN UP
            </span>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<!-- Login Modal -->
<%= render 'layouts/login_modal' %>
<!-- SignUP Modal -->
<%= render 'layouts/sign_up_modal' %>
