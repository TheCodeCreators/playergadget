<div class="form-group">
  <%= form_for(@tag, url: admin_tag_path(@tag.slug)) do |f| %>

    <div class="form-group">
      <label>
        <%= f.label :name %>
      </label>
      <%= f.text_field :name, class: 'form-control my-admin-area-modal-input' %>
    </div>

    <div class="form-group text-center">
      <%= f.submit class: 'btn darker-blue-btn' %>
    </div>
  <% end %>
</div>
