<%= form_for [:admin,  @ip], html: {role: :form} do |f| %>
  <% if @ip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ip.errors.count, "error") %> prohibited
        this ip from being saved:</h2>
        <ul>
        <% @ip.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :label %> 
    <%= f.text_field :label, class:"form-control my-admin-area-modal-input" %>
  </div>

  <div class="form-group">
    <%= f.label :addr %>
    <%= f.text_field :addr, class:"form-control my-admin-area-modal-input" %>
  </div>

  <div class="form-group text-center">
    <%= f.submit "Save Ip", :class=>'btn darker-blue-btn' %>
  </div>
<% end %>
